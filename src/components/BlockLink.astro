---
const props = Astro.props as { href: string };
---

<a
  href={props.href}
  class="flex flex-row gap-2 text-sm items-center p-1 rounded-sm border-2 border-black dark:border-white hover:border-primary"
  a-link
>
  <slot />
</a>
